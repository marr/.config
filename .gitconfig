[user]
	name = Dave Marr
	email = dave@crowdtilt.com
[color]
	ui = always
[alias]
    co = checkout
	lal = log --pretty=oneline --abbrev-commit --graph --decorate --all
	lol = log --pretty=oneline --abbrev-commit --graph --decorate
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
    ru = reset --hard @{upstream}
    st = status
    wip = !git add -u && git commit -m ...
	deploys = !git dlog --no-merges origin/master..origin/dev
	dlog = log --pretty=format:'%Cred%h%Creset - %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
[push]
	default = current
[help]
	autocorrect = 1
[hub]
	protocol = https
